import React from "react";
import MakeCard from "../MakeCard";

// This is where my MakeCard component is brought and dynamically created within my portfolio page
export default function Portfolio() {
  return (
    <div>
      <MakeCard />
    </div>
  );
}
